
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cart</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Your Cart</h1>
  </header>
  <main>
    <div class="cart-grid">
      <% cart.forEach(item => { %>
        <div class="cart-card">
          <img src="<%= item.image %>" alt="<%= item.name %>" class="product-image"/>
          <h2 class="product-name"><%= item.name %></h2>
          <p class="product-price">Price: €<%= item.price %></p>
          <p class="product-price">Quantity: <%= item.quantity %></p>
        </div>
      <% }) %>
    </div>
    <form action="/returnCart" method="POST" class="return-form">
      <button type="submit">Return to Procurement System</button>
    </form>
  </main>
</body>
</html>
